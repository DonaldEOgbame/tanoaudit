# Akira AI backend — core dependencies
# Pinned to the resolved, test-passing set (Python 3.14 .venv; the 3.12 Docker
# image installs the same versions). Bump deliberately, re-run the suite, repin.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# Database
sqlalchemy[asyncio]==2.0.50
alembic==1.18.4
asyncpg==0.31.0
aiosqlite==0.22.1          # local dev / tests

# Auth & crypto
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
pyotp==2.9.0
cryptography==48.0.1       # Fernet for API-key encryption
python-multipart==0.0.32   # form / file uploads

# Code parsing (boundary-aware segmentation)
tree-sitter==0.25.2
tree-sitter-language-pack==1.8.1

# Utilities
httpx==0.28.1

# Dev / test
pytest==9.0.3
pytest-asyncio==1.4.0
